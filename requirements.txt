fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# uvloop و httptools همون چیزیه که uvicorn با loop="auto"/http="auto" در main.py
# سعی می‌کنه استفاده کنه، ولی اگه نصب نباشن، بی‌سروصدا به پیاده‌سازی خالص پایتونی
# (asyncio loop معمولی + h11) برمی‌گرده که به‌طور محسوسی کندتره، مخصوصاً زیر بار
# زیاد (چند صد کانکشن هم‌زمان). این دو تا بزرگ‌ترین اهرم سرعتی هستن که کد به‌تنهایی
# نمی‌تونه فعالش کنه — باید این پکیج‌ها واقعاً نصب باشن.
uvloop>=0.21.0; sys_platform != "win32"
httptools>=0.6.0

httpx>=0.27.0
aiofiles>=24.1.0
